# 环境变量配置示例

## 说明
这些文件用于配置不同环境的变量。请根据实际需求修改，**不要将包含敏感信息的文件提交到 Git 仓库**。

---

## .env.development - 开发环境

```bash
# 应用基础路径
VITE_BASE_URL=/

# 开发服务器端口
VITE_PORT=3000

# 是否自动打开浏览器
VITE_OPEN=true

# API 代理配置（对应 vite.config.ts 中的 proxy）
VITE_API_BASE_URL=/api

# 是否移除 console
VITE_DROP_CONSOLE=false

# 是否移除 debugger
VITE_DROP_DEBUGGER=false

# 是否生成 sourcemap
VITE_SOURCEMAP=true

# 是否拆分 CSS
VITE_USE_CSS_SPLIT=false

# 输出目录
VITE_OUT_DIR=dist

# 其他自定义环境变量
# VITE_APP_TITLE=玄工智链
# VITE_APP_DESCRIPTION=玄工智链管理系统
```

---

## .env.production - 生产环境

```bash
# 应用基础路径（根据部署路径调整）
# 如果部署到根目录：VITE_BASE_URL=/
# 如果部署到子目录：VITE_BASE_URL=/starlink-mos-frontend/
VITE_BASE_URL=/

# 开发服务器端口（生产环境不使用）
VITE_PORT=3000

# 是否自动打开浏览器
VITE_OPEN=false

# API 基础地址（生产环境需要配置真实的 API 地址）
VITE_API_BASE_URL=https://api.your-domain.com

# 生产环境移除 console
VITE_DROP_CONSOLE=true

# 生产环境移除 debugger
VITE_DROP_DEBUGGER=true

# 生产环境不生成 sourcemap（为了安全）
VITE_SOURCEMAP=false

# 是否拆分 CSS
VITE_USE_CSS_SPLIT=false

# 输出目录
VITE_OUT_DIR=dist

# 其他自定义环境变量
# VITE_APP_TITLE=玄工智链
# VITE_APP_DESCRIPTION=玄工智链管理系统
```

---

## .env.staging - 预发布环境（可选）

```bash
# 应用基础路径
VITE_BASE_URL=/

# API 基础地址
VITE_API_BASE_URL=https://api-staging.your-domain.com

# 预发布环境可以保留 console 以便调试
VITE_DROP_CONSOLE=false

VITE_DROP_DEBUGGER=false

# 生成 sourcemap
VITE_SOURCEMAP=true

VITE_USE_CSS_SPLIT=false

VITE_OUT_DIR=dist-staging
```

---

## 在 Gitee 中配置环境变量

由于 .env 文件包含敏感信息，不应该提交到 Git。可以在 Gitee 的 CI/CD 配置中设置环境变量。

### 配置步骤：

1. 进入 Gitee 仓库
2. 点击「设置」→「流水线」→「环境变量」
3. 添加以下环境变量：

| 变量名 | 说明 | 示例值 |
|--------|------|--------|
| `VITE_BASE_URL` | 应用基础路径 | `/starlink-mos-frontend/` |
| `VITE_DROP_CONSOLE` | 是否移除 console | `true` |
| `VITE_DROP_DEBUGGER` | 是否移除 debugger | `true` |
| `VITE_SOURCEMAP` | 是否生成 sourcemap | `false` |
| `SSH_PRIVATE_KEY` | SSH 私钥（用于部署） | `-----BEGIN RSA PRIVATE KEY-----...` |
| `REMOTE_HOST` | 服务器地址 | `192.168.1.100` |
| `REMOTE_USER` | 服务器用户名 | `root` |
| `DINGTALK_WEBHOOK` | 钉钉通知 Webhook（可选） | `https://oapi.dingtalk.com/robot/send?access_token=...` |

---

## 生成 SSH 密钥（用于部署到服务器）

如果需要部署到自己的服务器，需要生成 SSH 密钥：

### 1. 在本地生成 SSH 密钥对

```bash
ssh-keygen -t rsa -b 4096 -C "gitee-ci"
# 按提示操作，可以不设置密码，直接回车
```

### 2. 将公钥添加到服务器

```bash
# 查看公钥内容
cat ~/.ssh/id_rsa.pub

# 登录到服务器，将公钥添加到 authorized_keys
ssh root@your-server-ip
cat >> ~/.ssh/authorized_keys < ~/.ssh/id_rsa.pub

# 设置正确的权限
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### 3. 将私钥添加到 Gitee 环境变量

```bash
# 查看私钥内容
cat ~/.ssh/id_rsa

# 复制全部内容（包括 BEGIN 和 END 标记）
```

在 Gitee 的环境变量中：
- 名称：`SSH_PRIVATE_KEY`
- 值：粘贴私钥的全部内容

---

## 本地开发使用

在本地开发时，创建 `.env.local` 文件（不要提交到 Git）：

```bash
# .env.local - 本地开发环境（优先级最高）
VITE_BASE_URL=/
VITE_PORT=3000
VITE_OPEN=true
VITE_API_BASE_URL=/api
```

### .gitignore 配置

确保 `.gitignore` 文件包含以下内容：

```gitignore
# 环境变量文件
.env.local
.env.*.local

# 依赖目录
vite-support/
node_modules/

# 构建产物
dist/
dist-*/

# 缓存
.cache/
.temp/
```

---

## 变量优先级

Vite 环境变量的优先级（从高到低）：

1. 命令行中指定的变量
2. `.env.[mode].local`
3. `.env.local`
4. `.env.[mode]`
5. `.env`

例如，开发环境启动时：
- 会加载 `.env.development`
- 如果存在 `.env.development.local`，会覆盖 `.env.development` 中的变量
- 如果存在 `.env.local`，会覆盖上面的变量

---

## 在代码中使用环境变量

```typescript
// 在 Vue 组件中使用
const apiUrl = import.meta.env.VITE_API_BASE_URL

// 在 TypeScript 中使用类型声明
// vite-env.d.ts
interface ImportMetaEnv {
  readonly VITE_BASE_URL: string
  readonly VITE_PORT: string
  readonly VITE_OPEN: string
  readonly VITE_API_BASE_URL: string
  readonly VITE_DROP_CONSOLE: string
  readonly VITE_DROP_DEBUGGER: string
  readonly VITE_SOURCEMAP: string
  readonly VITE_USE_CSS_SPLIT: string
  readonly VITE_OUT_DIR: string
}

interface ImportMeta {
  readonly env: ImportMetaEnv
}
```

---

## 常见问题

### Q: 为什么构建时环境变量没有生效？

A: 确保变量名以 `VITE_` 开头，Vite 只会暴露以 `VITE_` 开头的变量到客户端代码。

### Q: 如何区分不同环境的 API 地址？

A: 使用不同的 `.env` 文件，例如：
- `.env.development`: `VITE_API_BASE_URL=http://localhost:8080`
- `.env.production`: `VITE_API_BASE_URL=https://api.example.com`

### Q: Gitee CI 中的环境变量优先级？

A: Gitee CI 中配置的环境变量会覆盖 `.env` 文件中的同名变量。
