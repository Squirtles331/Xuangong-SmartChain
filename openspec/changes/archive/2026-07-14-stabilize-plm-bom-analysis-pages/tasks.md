## 1. 变更文档

- [x] 1.1 创建 `PLM BOM` 分析页的 proposal、design、tasks 和 capability spec

## 2. 路由与归属

- [x] 2.1 为 `结构版本比较` 补齐 `PLM` 路由归属、核心对象和边界说明
- [x] 2.2 为 `BOM展开/反查` 补齐 `PLM` 路由归属、核心对象和边界说明

## 3. 页面实现

- [x] 3.1 重写 `结构版本比较` 页面，补齐查询区、概览卡片、差异结果表和详情抽屉
- [x] 3.2 为 `结构版本比较` 增加静态导出动作和差异状态表达
- [x] 3.3 重写 `BOM展开/反查` 页面，补齐统一查询区、模式切换、概览卡片和结果表
- [x] 3.4 为 `BOM展开/反查` 增加节点详情抽屉，固定版本、替代、库存和路线影响区块
- [x] 3.5 完成类型检查与构建验证
