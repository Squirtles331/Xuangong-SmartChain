## 1. 变更文档

- [x] 1.1 创建 `PLM` 工序定义静态页的 proposal、design、tasks 和 capability spec
- [x] 1.2 明确 `MDM` 与 `PLM` 在工序/工艺路线维护权、消费权和编码权上的分责

## 2. 路由与归属

- [x] 2.1 在 `PLM` 菜单下新增工序定义主入口并补齐页面归属元信息
- [x] 2.2 从 `MDM` 菜单中移除工序维护入口，确保 `MDM` 不承担工序定义的新增、修改、删除责任
- [x] 2.3 明确 `工序编码`、`工艺路线编码` 的规则入口保留在 `MDM`，实例维护入口只保留在 `PLM`
- [x] 2.4 从 `MDM` 菜单中移除 `BOM`、`工艺路线`、`工艺参数` 的重复维护入口，收敛为 `PLM` 唯一维护入口
- [x] 2.5 清理 `MDM` 下已失效的 `bom-master`、`routing-master`、`operation-master`、`process-parameter` 遗留页面文件

## 3. 页面实现

- [x] 3.1 实现 `PLM` 工序定义静态页的查询区、概览卡片、列表区和状态表达
- [x] 3.2 实现工序定义详情抽屉，补齐工时资源、质量报工约束和上下游关联区
- [x] 3.3 完成新增、编辑、启停用、删除等静态交互并执行构建验证
- [x] 3.4 在保存或发布口径中预留 `PLM` 对编码实例唯一性校验的实现位置
