## 1. 变更文档

- [x] 1.1 创建 `PLM` 工艺路线子页基线对齐变更文档

## 2. 路由与归属

- [x] 2.1 为 `并行工序` 补齐 `PLM` 路由归属、核心对象和边界说明
- [x] 2.2 为 `标准工时学习` 补齐 `PLM` 路由归属、核心对象和边界说明

## 3. 页面实现

- [x] 3.1 将 `并行工序` 页面重构为 `CrudPage` 基线
- [x] 3.2 将 `标准工时学习` 页面重构为 `CrudPage` 基线
- [x] 3.3 新增 `PLM` 菜单到文件路径映射文档
- [x] 3.4 完成 lint、类型检查和构建验证
