## 1. 基线收敛

- [ ] 1.1 将当前 `docs/` 中的对象目录、字段标准、状态流和页面路线图整理为统一开发参考入口
- [ ] 1.2 为第一阶段核心对象建立页面-对象-字段-状态对照清单
- [ ] 1.3 为静态页评审建立统一检查项，覆盖页面结构、字段分组、状态标签、操作按钮和详情区块

## 2. MDM 主数据静态页

- [ ] 2.1 按统一对象基线重构 `物料` 静态页的查询区、列表、表单分组和状态表达
- [ ] 2.2 按统一对象基线重构 `仓库/库位` 静态页的结构和静态数据
- [ ] 2.3 按统一对象基线重构 `客户/供应商` 静态页的结构和静态数据
- [ ] 2.4 为 `MDM` 第一批页面沉淀统一的静态数据结构与字段映射说明

## 3. PLM 定义页静态化

- [ ] 3.1 按统一对象基线重构 `EBOM/MBOM` 页面结构、版本表达和状态标签
- [ ] 3.2 按统一对象基线重构 `工艺路线/工序定义` 页面结构和详情区块
- [ ] 3.3 按统一对象基线重构 `ECN` 页面结构和状态流展示
- [ ] 3.4 为 `PLM` 第一批页面沉淀统一的静态数据结构与字段映射说明

## 4. 执行链路入口页准备

- [ ] 4.1 基于已稳定的上游对象定义，设计 `工单` 页面静态结构与头行区块
- [ ] 4.2 基于工单结构，设计 `报工` 页面静态结构、工序执行区块和状态表达
- [ ] 4.3 基于执行链路，设计 `领料/退料/入库` 页面静态结构与事务关系区块
- [ ] 4.4 基于执行对象，设计 `来料/过程/完工检验` 页面静态结构与裁决区块

## 5. mock 与 API 交接准备

- [ ] 5.1 为第一阶段页面批次确认静态页评审通过清单，作为进入 mock 的前置门禁
- [ ] 5.2 为 `MDM` 与 `PLM` 第一批页面设计 mock 列表、详情和状态流转数据结构
- [ ] 5.3 为执行链路页面设计 mock 的头行、明细、关系和轨迹数据结构
- [ ] 5.4 基于已稳定的 mock 结构输出后续 API DTO 设计清单和边界说明
